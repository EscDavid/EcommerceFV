// scripts.js
/*document.addEventListener('DOMContentLoaded', function() {
    // Obtener el icono y el formulario de inicio de sesión flotante
    var loginIcon = document.getElementById('login-icon');
    var floatingLogin = document.getElementById('floating-login');
  
    // Al hacer clic en el icono, agregar la clase "show" al formulario flotante
    loginIcon.addEventListener('click', function(event) {
      event.preventDefault();
      floatingLogin.classList.add('show');
    });
  
    // Al hacer clic fuera del formulario, quitar la clase "show"
    document.addEventListener('click', function(event) {
      var target = event.target;
  
      if (!floatingLogin.contains(target) && target !== loginIcon) {
        floatingLogin.classList.remove('show');
      }
    });
  });*/
  